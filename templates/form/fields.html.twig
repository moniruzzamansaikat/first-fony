{# Define the custom form widget rendering #}
{% block form_row %}
    <div class="form-group mb-3">
        {{ form_label(form) }}
        {{ form_widget(form, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form) }}
    </div>
{% endblock %}