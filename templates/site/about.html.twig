{% extends 'layouts/site.html.twig' %}

{% block content %}

<div class="row">
    <div class="col-md-4 offset-4 my-5">
        <div class="card p-4">
            <h4>✅ Software Version: {{ softwareVersion }}!</h4>
            <h4>✅ PHP Version: {{ phpVersion }}!</h4>
            <h4>✅ Symfony Version: {{ frameworkVersion }}!</h4>
            <h4>✅ Timezone: {{ timeZone }}!</h4>
        </div>
    </div>
</div>

{% endblock %}
